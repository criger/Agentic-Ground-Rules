# Community Events Web example

This is a fictional example, not a source repository.

- Read `README.md`, `context.md`, `architecture.md` and `current-status.md`.
- Read `api.md` before changing requests, authentication, polling or response mapping.
- Read `pitfalls.md` before changing effects, browser storage or route guards.
- Treat client-side access checks as presentation only; the API is authoritative.
- Preserve `credentials: 'include'` and the documented storage-key version unless the contract is intentionally migrated.
- Do not claim that fictional repositories or verification results are real.
