# Gemini adapter

Read and follow the root `AGENTS.md`.

When working in a nested directory, also read the relevant `AGENTS.md` files from the repository root down to that directory. The closest file specializes the broader guidance.

Do not duplicate policy here. The `AGENTS.md` hierarchy is the canonical source.
